name: DREAM Pixel-Encoded Codebase Atlas
run-name: DREAM Pixel-Encoded Codebase Atlas

"on":
  workflow_dispatch:
    inputs:
      maximum_dimension:
        description: "Maximum width/height of the single RGB recovery PNG"
        required: false
        default: "16000"
        type: string

permissions:
  contents: read

concurrency:
  group: dream-pixel-codebase-atlas-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-pixel-codebase-atlas:
    name: Encode and prove the codebase from one PNG
    runs-on: ubuntu-latest
    timeout-minutes: 240

    steps:
      - name: Checkout tracked repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          lfs: true
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install the image encoder runtime
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --disable-pip-version-check \
            "Pillow>=10,<13" \
            "numpy>=2,<3"

      - name: Write the decoder embedded into the PNG
        shell: bash
        run: |
          set -euo pipefail
          cat > "$RUNNER_TEMP/dream_pixel_restore.py" <<'PY'
          #!/usr/bin/env python3
          from __future__ import annotations

          import argparse
          import base64
          import hashlib
          import json
          import os
          import stat
          import struct
          import sys
          import zlib
          from pathlib import Path, PurePosixPath
          from typing import Iterator

          PNG_SIGNATURE = b"\x89PNG\r\n\x1a\n"
          PACKET_MAGIC = b"DRMPXL01"
          PACKET_PREFIX = struct.Struct(">8sI")
          CAPSULE_MAGIC = b"DRMCAP01"
          CAPSULE_PREFIX = struct.Struct(">8sQ")


          class RecoveryError(RuntimeError):
              pass


          def sha256(data: bytes) -> str:
              return hashlib.sha256(data).hexdigest()


          def paeth(a: int, b: int, c: int) -> int:
              p = a + b - c
              pa = abs(p - a)
              pb = abs(p - b)
              pc = abs(p - c)
              if pa <= pb and pa <= pc:
                  return a
              if pb <= pc:
                  return b
              return c


          def unfilter_row(
              filter_type: int,
              row: bytearray,
              previous: bytes,
              bytes_per_pixel: int,
          ) -> bytes:
              if filter_type == 0:
                  return bytes(row)

              for index in range(len(row)):
                  left = row[index - bytes_per_pixel] if index >= bytes_per_pixel else 0
                  up = previous[index] if previous else 0
                  upper_left = (
                      previous[index - bytes_per_pixel]
                      if previous and index >= bytes_per_pixel
                      else 0
                  )

                  if filter_type == 1:
                      row[index] = (row[index] + left) & 0xFF
                  elif filter_type == 2:
                      row[index] = (row[index] + up) & 0xFF
                  elif filter_type == 3:
                      row[index] = (row[index] + ((left + up) // 2)) & 0xFF
                  elif filter_type == 4:
                      row[index] = (
                          row[index] + paeth(left, up, upper_left)
                      ) & 0xFF
                  else:
                      raise RecoveryError(
                          f"unsupported PNG filter type: {filter_type}"
                      )

              return bytes(row)


          def iter_png_rgb_rows(image_path: Path) -> Iterator[bytes]:
              width = None
              height = None
              stride = None
              previous = b""
              pending = bytearray()
              rows_seen = 0
              decompressor = zlib.decompressobj()

              with image_path.open("rb") as handle:
                  if handle.read(8) != PNG_SIGNATURE:
                      raise RecoveryError(
                          "input is not a PNG; use the original generated artifact"
                      )

                  while True:
                      raw_length = handle.read(4)
                      if not raw_length:
                          break
                      if len(raw_length) != 4:
                          raise RecoveryError("truncated PNG chunk length")

                      length = struct.unpack(">I", raw_length)[0]
                      chunk_type = handle.read(4)
                      data = handle.read(length)
                      raw_crc = handle.read(4)

                      if (
                          len(chunk_type) != 4
                          or len(data) != length
                          or len(raw_crc) != 4
                      ):
                          raise RecoveryError("truncated PNG chunk")

                      expected_crc = struct.unpack(">I", raw_crc)[0]
                      actual_crc = zlib.crc32(chunk_type)
                      actual_crc = zlib.crc32(data, actual_crc) & 0xFFFFFFFF
                      if expected_crc != actual_crc:
                          raise RecoveryError(
                              f"PNG CRC mismatch in {chunk_type!r}"
                          )

                      if chunk_type == b"IHDR":
                          if length != 13:
                              raise RecoveryError("invalid PNG IHDR length")

                          (
                              width,
                              height,
                              bit_depth,
                              color_type,
                              compression_method,
                              filter_method,
                              interlace_method,
                          ) = struct.unpack(">IIBBBBB", data)

                          if (
                              bit_depth != 8
                              or color_type != 2
                              or compression_method != 0
                              or filter_method != 0
                              or interlace_method != 0
                          ):
                              raise RecoveryError(
                                  "atlas must remain a non-interlaced 8-bit RGB PNG"
                              )

                          stride = width * 3

                      elif chunk_type == b"IDAT":
                          if stride is None:
                              raise RecoveryError("PNG IDAT appeared before IHDR")

                          pending.extend(decompressor.decompress(data))
                          row_size = stride + 1

                          while len(pending) >= row_size:
                              filter_type = pending[0]
                              filtered = bytearray(pending[1:row_size])
                              del pending[:row_size]

                              restored = unfilter_row(
                                  filter_type,
                                  filtered,
                                  previous,
                                  3,
                              )
                              previous = restored
                              rows_seen += 1
                              yield restored

                      elif chunk_type == b"IEND":
                          if stride is None or height is None:
                              raise RecoveryError("PNG ended before IHDR")

                          pending.extend(decompressor.flush())
                          row_size = stride + 1

                          while (
                              len(pending) >= row_size
                              and rows_seen < height
                          ):
                              filter_type = pending[0]
                              filtered = bytearray(pending[1:row_size])
                              del pending[:row_size]

                              restored = unfilter_row(
                                  filter_type,
                                  filtered,
                                  previous,
                                  3,
                              )
                              previous = restored
                              rows_seen += 1
                              yield restored

                          if rows_seen != height:
                              raise RecoveryError(
                                  "PNG row count mismatch: "
                                  f"expected {height}, decoded {rows_seen}"
                              )
                          return

              raise RecoveryError("PNG is missing its IEND chunk")


          def iter_carrier_bytes(image_path: Path) -> Iterator[int]:
              accumulator = 0
              bit_count = 0

              for row in iter_png_rgb_rows(image_path):
                  for index in range(0, len(row), 3):
                      symbol = (
                          ((row[index] & 0x03) << 4)
                          | ((row[index + 1] & 0x03) << 2)
                          | (row[index + 2] & 0x03)
                      )

                      accumulator = (accumulator << 6) | symbol
                      bit_count += 6

                      while bit_count >= 8:
                          bit_count -= 8
                          yield (accumulator >> bit_count) & 0xFF
                          accumulator &= (
                              (1 << bit_count) - 1 if bit_count else 0
                          )


          def read_exact(stream: Iterator[int], count: int) -> bytes:
              output = bytearray()

              for _ in range(count):
                  try:
                      output.append(next(stream))
                  except StopIteration as error:
                      raise RecoveryError(
                          f"pixel carrier ended while reading {count} bytes"
                      ) from error

              return bytes(output)


          def decode_image_packet(image_path: Path) -> tuple[dict, bytes]:
              stream = iter_carrier_bytes(image_path)

              prefix = read_exact(stream, PACKET_PREFIX.size)
              magic, header_length = PACKET_PREFIX.unpack(prefix)

              if magic != PACKET_MAGIC:
                  raise RecoveryError(
                      "DREAM pixel magic is absent. The PNG was probably "
                      "resized, recompressed, converted, or screenshotted."
                  )

              encoded_header = read_exact(stream, header_length)
              try:
                  header = json.loads(encoded_header.decode("utf-8"))
              except Exception as error:
                  raise RecoveryError(
                      f"invalid pixel packet header: {error}"
                  ) from error

              payload_length = int(header.get("payloadBytes", -1))
              if payload_length < 0:
                  raise RecoveryError("pixel packet is missing payloadBytes")

              payload = read_exact(stream, payload_length)

              if sha256(payload) != header.get("payloadSha256"):
                  raise RecoveryError(
                      "pixel payload SHA-256 mismatch"
                  )

              return header, payload


          def parse_capsule(payload: bytes) -> tuple[dict, memoryview]:
              if len(payload) < CAPSULE_PREFIX.size:
                  raise RecoveryError("truncated codebase capsule")

              magic, manifest_length = CAPSULE_PREFIX.unpack(
                  payload[: CAPSULE_PREFIX.size]
              )
              if magic != CAPSULE_MAGIC:
                  raise RecoveryError("invalid codebase capsule magic")

              manifest_start = CAPSULE_PREFIX.size
              manifest_end = manifest_start + manifest_length
              if manifest_end > len(payload):
                  raise RecoveryError("truncated codebase manifest")

              try:
                  manifest = json.loads(
                      payload[manifest_start:manifest_end].decode("utf-8")
                  )
              except Exception as error:
                  raise RecoveryError(
                      f"invalid codebase manifest: {error}"
                  ) from error

              embedded_decoder = zlib.decompress(
                  base64.b64decode(
                      manifest["embeddedDecoderZlibBase64"],
                      validate=True,
                  )
              )
              if sha256(embedded_decoder) != manifest["embeddedDecoderSha256"]:
                  raise RecoveryError(
                      "decoder copy inside the pixel payload is corrupt"
                  )

              return manifest, memoryview(payload)[manifest_end:]


          def validated_path(value: str) -> PurePosixPath:
              path = PurePosixPath(value)

              if (
                  not value
                  or path.is_absolute()
                  or any(part in {"", ".", ".."} for part in path.parts)
              ):
                  raise RecoveryError(f"unsafe archive path: {value!r}")

              return path


          def validate_symlink_target(
              link_path: PurePosixPath,
              target: str,
          ) -> None:
              if not target or "\x00" in target:
                  raise RecoveryError(
                      f"invalid symlink target for {link_path}"
                  )

              target_path = PurePosixPath(target)
              if target_path.is_absolute():
                  raise RecoveryError(
                      f"absolute symlink is not allowed: "
                      f"{link_path} -> {target}"
                  )

              depth = len(link_path.parent.parts)
              for part in target_path.parts:
                  if part in {"", "."}:
                      continue
                  if part == "..":
                      depth -= 1
                      if depth < 0:
                          raise RecoveryError(
                              f"symlink escapes output tree: "
                              f"{link_path} -> {target}"
                          )
                  else:
                      depth += 1


          def read_file_entry(
              entry: dict,
              data: memoryview,
          ) -> bytes:
              offset = int(entry["offset"])
              compressed_size = int(entry["compressedSize"])
              raw_size = int(entry["size"])

              if (
                  offset < 0
                  or compressed_size < 0
                  or offset + compressed_size > len(data)
              ):
                  raise RecoveryError(
                      f"invalid payload interval for {entry.get('path')}"
                  )

              compressed = bytes(
                  data[offset : offset + compressed_size]
              )

              if sha256(compressed) != entry["compressedSha256"]:
                  raise RecoveryError(
                      f"compressed SHA-256 mismatch for {entry.get('path')}"
                  )

              try:
                  raw = zlib.decompress(compressed)
              except Exception as error:
                  raise RecoveryError(
                      f"cannot decompress {entry.get('path')}: {error}"
                  ) from error

              if len(raw) != raw_size:
                  raise RecoveryError(
                      f"size mismatch for {entry.get('path')}"
                  )

              if sha256(raw) != entry["sha256"]:
                  raise RecoveryError(
                      f"content SHA-256 mismatch for {entry.get('path')}"
                  )

              return raw


          def validate_manifest(
              manifest: dict,
              data: memoryview,
              verify_all_content: bool,
          ) -> None:
              entries = manifest.get("entries")
              if not isinstance(entries, list):
                  raise RecoveryError("manifest entries are missing")

              seen: set[str] = set()
              symlinks: set[PurePosixPath] = set()

              root_descriptor = []

              for entry in entries:
                  relative = validated_path(
                      str(entry.get("path", ""))
                  )
                  key = relative.as_posix()

                  if key in seen:
                      raise RecoveryError(
                          f"duplicate manifest path: {key}"
                      )
                  seen.add(key)

                  for parent in relative.parents:
                      if parent in symlinks:
                          raise RecoveryError(
                              f"path traverses a symlink parent: {key}"
                          )

                  kind = entry.get("type")
                  if kind == "file":
                      if verify_all_content:
                          read_file_entry(entry, data)

                      root_descriptor.append(
                          {
                              "path": key,
                              "type": "file",
                              "mode": int(entry["mode"]),
                              "size": int(entry["size"]),
                              "sha256": entry["sha256"],
                              "target": None,
                          }
                      )

                  elif kind == "symlink":
                      target = str(entry.get("target", ""))
                      validate_symlink_target(relative, target)
                      symlinks.add(relative)

                      root_descriptor.append(
                          {
                              "path": key,
                              "type": "symlink",
                              "mode": int(entry["mode"]),
                              "size": None,
                              "sha256": None,
                              "target": target,
                          }
                      )

                  else:
                      raise RecoveryError(
                          f"unsupported entry type for {key}: {kind!r}"
                      )

              encoded_root = json.dumps(
                  root_descriptor,
                  sort_keys=True,
                  separators=(",", ":"),
                  ensure_ascii=False,
              ).encode("utf-8")

              if sha256(encoded_root) != manifest["codebaseRoot"]:
                  raise RecoveryError("codebase root mismatch")


          def safe_output_path(
              root: Path,
              relative: PurePosixPath,
          ) -> Path:
              root_resolved = root.resolve()
              target = root.joinpath(*relative.parts)

              current = root
              for part in relative.parts[:-1]:
                  current = current / part
                  if current.exists() and current.is_symlink():
                      raise RecoveryError(
                          f"output path traverses symlink: {relative}"
                      )

              parent = target.parent.resolve()
              if (
                  parent != root_resolved
                  and root_resolved not in parent.parents
              ):
                  raise RecoveryError(
                      f"output path escapes destination: {relative}"
                  )

              return target


          def restore(
              image_path: Path,
              output: Path,
          ) -> dict:
              header, payload = decode_image_packet(image_path)
              manifest, data = parse_capsule(payload)
              validate_manifest(
                  manifest,
                  data,
                  verify_all_content=False,
              )

              if output.exists() and any(output.iterdir()):
                  raise RecoveryError(
                      f"destination is not empty: {output}"
                  )

              output.mkdir(parents=True, exist_ok=True)
              entries = manifest["entries"]

              restored_files = 0
              restored_symlinks = 0

              for entry in entries:
                  if entry["type"] != "file":
                      continue

                  relative = validated_path(entry["path"])
                  destination = safe_output_path(output, relative)
                  destination.parent.mkdir(
                      parents=True,
                      exist_ok=True,
                  )
                  destination.write_bytes(
                      read_file_entry(entry, data)
                  )
                  os.chmod(
                      destination,
                      int(entry["mode"]) & 0o777,
                  )
                  restored_files += 1

              for entry in entries:
                  if entry["type"] != "symlink":
                      continue

                  relative = validated_path(entry["path"])
                  destination = safe_output_path(output, relative)
                  destination.parent.mkdir(
                      parents=True,
                      exist_ok=True,
                  )
                  os.symlink(
                      entry["target"],
                      destination,
                  )
                  restored_symlinks += 1

              return {
                  "restored": True,
                  "format": header.get("format"),
                  "codebaseRoot": manifest["codebaseRoot"],
                  "files": restored_files,
                  "symlinks": restored_symlinks,
                  "output": str(output),
              }


          def extract_one(
              image_path: Path,
              requested_path: str,
              destination: Path,
          ) -> dict:
              _, payload = decode_image_packet(image_path)
              manifest, data = parse_capsule(payload)
              wanted = validated_path(requested_path).as_posix()

              for entry in manifest["entries"]:
                  if entry["path"] != wanted:
                      continue

                  destination.parent.mkdir(
                      parents=True,
                      exist_ok=True,
                  )

                  if entry["type"] == "file":
                      destination.write_bytes(
                          read_file_entry(entry, data)
                      )
                      os.chmod(
                          destination,
                          int(entry["mode"]) & 0o777,
                      )
                  else:
                      validate_symlink_target(
                          PurePosixPath(wanted),
                          entry["target"],
                      )
                      os.symlink(
                          entry["target"],
                          destination,
                      )

                  return {
                      "extracted": True,
                      "path": wanted,
                      "destination": str(destination),
                      "type": entry["type"],
                  }

              raise RecoveryError(
                  f"path is not present in the atlas: {wanted}"
              )


          def main() -> int:
              parser = argparse.ArgumentParser(
                  description=(
                      "Restore a complete tracked codebase from the RGB "
                      "low-bit payload of one DREAM PNG."
                  )
              )
              commands = parser.add_subparsers(
                  dest="command",
                  required=True,
              )

              restore_parser = commands.add_parser("restore")
              restore_parser.add_argument("image", type=Path)
              restore_parser.add_argument("output", type=Path)

              verify_parser = commands.add_parser("verify")
              verify_parser.add_argument("image", type=Path)

              list_parser = commands.add_parser("list")
              list_parser.add_argument("image", type=Path)

              extract_parser = commands.add_parser("extract")
              extract_parser.add_argument("image", type=Path)
              extract_parser.add_argument("path")
              extract_parser.add_argument("destination", type=Path)

              args = parser.parse_args()

              if args.command == "restore":
                  result = restore(
                      args.image.resolve(),
                      args.output.resolve(),
                  )

              elif args.command == "verify":
                  header, payload = decode_image_packet(
                      args.image.resolve()
                  )
                  manifest, data = parse_capsule(payload)
                  validate_manifest(
                      manifest,
                      data,
                      verify_all_content=True,
                  )
                  result = {
                      "valid": True,
                      "format": header.get("format"),
                      "codebaseRoot": manifest["codebaseRoot"],
                      "entries": len(manifest["entries"]),
                      "payloadBytes": len(payload),
                  }

              elif args.command == "list":
                  _, payload = decode_image_packet(
                      args.image.resolve()
                  )
                  manifest, _ = parse_capsule(payload)
                  for entry in manifest["entries"]:
                      print(entry["path"])
                  return 0

              else:
                  result = extract_one(
                      args.image.resolve(),
                      args.path,
                      args.destination.resolve(),
                  )

              print(
                  json.dumps(
                      result,
                      indent=2,
                      sort_keys=True,
                  )
              )
              return 0


          if __name__ == "__main__":
              try:
                  raise SystemExit(main())
              except (
                  RecoveryError,
                  KeyError,
                  OSError,
                  ValueError,
                  zlib.error,
              ) as error:
                  print(
                      f"DREAM recovery failed: {error}",
                      file=sys.stderr,
                  )
                  raise SystemExit(2)
          PY

          python -m py_compile "$RUNNER_TEMP/dream_pixel_restore.py"

      - name: Encode every tracked byte into RGB pixels
        shell: bash
        env:
          MAXIMUM_DIMENSION: ${{ inputs.maximum_dimension }}
        run: |
          set -euo pipefail
          cat > "$RUNNER_TEMP/build_dream_pixel_atlas.py" <<'PY'
          #!/usr/bin/env python3
          from __future__ import annotations

          import argparse
          import base64
          import hashlib
          import json
          import math
          import os
          import shutil
          import stat
          import struct
          import subprocess
          import tempfile
          import zlib
          from pathlib import Path, PurePosixPath

          import numpy as np
          from PIL import Image, ImageDraw

          PACKET_MAGIC = b"DRMPXL01"
          PACKET_PREFIX = struct.Struct(">8sI")
          CAPSULE_MAGIC = b"DRMCAP01"
          CAPSULE_PREFIX = struct.Struct(">8sQ")
          CARRIER_BITS_PER_PIXEL = 6
          VISIBLE_CHANNEL_MASK = 0xFC


          def sha256(data: bytes) -> str:
              return hashlib.sha256(data).hexdigest()


          def sha256_file(path: Path) -> str:
              digest = hashlib.sha256()
              with path.open("rb") as handle:
                  while True:
                      chunk = handle.read(1024 * 1024)
                      if not chunk:
                          break
                      digest.update(chunk)
              return digest.hexdigest()


          def canonical_json(value: object) -> bytes:
              return json.dumps(
                  value,
                  sort_keys=True,
                  separators=(",", ":"),
                  ensure_ascii=False,
              ).encode("utf-8")


          def tracked_paths(root: Path) -> list[str]:
              result = subprocess.run(
                  [
                      "git",
                      "-C",
                      str(root),
                      "ls-files",
                      "-z",
                      "--recurse-submodules",
                  ],
                  check=True,
                  stdout=subprocess.PIPE,
              )
              paths = [
                  item.decode("utf-8", "surrogateescape")
                  for item in result.stdout.split(b"\0")
                  if item
              ]
              paths = sorted(set(paths))
              if not paths:
                  raise RuntimeError("git reported no tracked paths")
              return paths


          def validated_repo_path(value: str) -> PurePosixPath:
              path = PurePosixPath(value)
              if (
                  not value
                  or path.is_absolute()
                  or any(part in {"", ".", ".."} for part in path.parts)
              ):
                  raise RuntimeError(
                      f"unsafe repository path: {value!r}"
                  )
              return path


          def validate_symlink_target(
              link_path: PurePosixPath,
              target: str,
          ) -> None:
              if not target or "\x00" in target:
                  raise RuntimeError(
                      f"invalid symlink target for {link_path}"
                  )

              target_path = PurePosixPath(target)
              if target_path.is_absolute():
                  raise RuntimeError(
                      f"absolute symlink is not recoverable safely: "
                      f"{link_path} -> {target}"
                  )

              depth = len(link_path.parent.parts)
              for part in target_path.parts:
                  if part in {"", "."}:
                      continue
                  if part == "..":
                      depth -= 1
                      if depth < 0:
                          raise RuntimeError(
                              f"symlink escapes repository: "
                              f"{link_path} -> {target}"
                          )
                  else:
                      depth += 1


          def build_capsule(
              root: Path,
              destination: Path,
              decoder_source: bytes,
              temporary: Path,
          ) -> tuple[dict, int]:
              data_path = temporary / "codebase-data.bin"
              entries: list[dict] = []
              offset = 0

              with data_path.open("wb") as data_handle:
                  for relative_text in tracked_paths(root):
                      relative = validated_repo_path(relative_text)
                      absolute = root.joinpath(*relative.parts)

                      if (
                          not absolute.exists()
                          and not absolute.is_symlink()
                      ):
                          raise RuntimeError(
                              "tracked path is missing from checkout: "
                              f"{relative_text}"
                          )

                      metadata = os.lstat(absolute)
                      mode = stat.S_IMODE(metadata.st_mode)

                      if stat.S_ISDIR(metadata.st_mode):
                          continue

                      if stat.S_ISLNK(metadata.st_mode):
                          target = os.readlink(absolute)
                          validate_symlink_target(
                              relative,
                              target,
                          )
                          entries.append(
                              {
                                  "path": relative.as_posix(),
                                  "type": "symlink",
                                  "mode": mode,
                                  "target": target,
                              }
                          )
                          continue

                      if not stat.S_ISREG(metadata.st_mode):
                          raise RuntimeError(
                              "unsupported tracked file type: "
                              f"{relative_text}"
                          )

                      raw = absolute.read_bytes()
                      compressed = zlib.compress(raw, level=9)

                      entries.append(
                          {
                              "path": relative.as_posix(),
                              "type": "file",
                              "mode": mode,
                              "size": len(raw),
                              "sha256": sha256(raw),
                              "codec": "zlib",
                              "offset": offset,
                              "compressedSize": len(compressed),
                              "compressedSha256": sha256(compressed),
                          }
                      )

                      data_handle.write(compressed)
                      offset += len(compressed)

              root_descriptor = [
                  {
                      "path": entry["path"],
                      "type": entry["type"],
                      "mode": entry["mode"],
                      "size": entry.get("size"),
                      "sha256": entry.get("sha256"),
                      "target": entry.get("target"),
                  }
                  for entry in entries
              ]
              codebase_root = sha256(
                  canonical_json(root_descriptor)
              )

              manifest = {
                  "format": "dream-pixel-codebase-capsule",
                  "version": 1,
                  "codebaseRoot": codebase_root,
                  "entryCount": len(entries),
                  "dataBytes": offset,
                  "embeddedDecoderSha256": sha256(decoder_source),
                  "embeddedDecoderZlibBase64": base64.b64encode(
                      zlib.compress(decoder_source, level=9)
                  ).decode("ascii"),
                  "entries": entries,
              }
              manifest_bytes = canonical_json(manifest)

              with destination.open("wb") as capsule_handle:
                  capsule_handle.write(
                      CAPSULE_PREFIX.pack(
                          CAPSULE_MAGIC,
                          len(manifest_bytes),
                      )
                  )
                  capsule_handle.write(manifest_bytes)

                  with data_path.open("rb") as data_handle:
                      shutil.copyfileobj(
                          data_handle,
                          capsule_handle,
                          length=1024 * 1024,
                      )

              return manifest, len(manifest_bytes)


          def quantized_color(
              seed: str,
              brightness: int = 0,
          ) -> tuple[int, int, int]:
              digest = hashlib.sha256(
                  seed.encode("utf-8")
              ).digest()
              channels = (
                  56 + digest[0] % 164,
                  56 + digest[7] % 164,
                  56 + digest[13] % 164,
              )
              return tuple(
                  min(
                      252,
                      max(
                          0,
                          ((value + brightness) // 4) * 4,
                      ),
                  )
                  for value in channels
              )


          def superformula_radius(
              theta: np.ndarray,
              m: float,
              n1: float,
              n2: float,
              n3: float,
              a: float = 1.0,
              b: float = 1.0,
          ) -> np.ndarray:
              first = np.abs(
                  np.cos(m * theta / 4.0) / a
              ) ** n2
              second = np.abs(
                  np.sin(m * theta / 4.0) / b
              ) ** n3
              return (
                  first + second + 1.0e-8
              ) ** (-1.0 / n1)


          def rotation_4d(
              points: np.ndarray,
              plane_a: int,
              plane_b: int,
              angle: float,
          ) -> np.ndarray:
              output = points.copy()
              cosine = math.cos(angle)
              sine = math.sin(angle)
              first = points[:, plane_a]
              second = points[:, plane_b]
              output[:, plane_a] = (
                  cosine * first - sine * second
              )
              output[:, plane_b] = (
                  sine * first + cosine * second
              )
              return output


          def transform_4d(
              points: np.ndarray,
              angles: tuple[float, ...],
          ) -> np.ndarray:
              output = points.astype(
                  np.float64,
                  copy=True,
              )
              planes = (
                  (0, 1),
                  (2, 3),
                  (0, 3),
                  (1, 2),
                  (0, 2),
                  (1, 3),
              )
              for (
                  plane_a,
                  plane_b,
              ), angle in zip(planes, angles):
                  output = rotation_4d(
                      output,
                      plane_a,
                      plane_b,
                      angle,
                  )
              return output


          def project_4d(
              points: np.ndarray,
              angles: tuple[float, ...],
              center: tuple[float, float],
              scale: float,
          ) -> list[tuple[int, int]]:
              rotated = transform_4d(
                  points,
                  angles,
              )
              denominator_4d = np.maximum(
                  0.45,
                  2.35 - rotated[:, 3],
              )
              xyz = rotated[:, :3] / denominator_4d[:, None]
              denominator_3d = np.maximum(
                  0.55,
                  3.15 - xyz[:, 2],
              )
              xy = xyz[:, :2] / denominator_3d[:, None]
              projected = np.column_stack(
                  (
                      center[0] + scale * xy[:, 0],
                      center[1] + scale * xy[:, 1],
                  )
              )
              return [
                  (
                      int(round(x_value)),
                      int(round(y_value)),
                  )
                  for x_value, y_value in projected
              ]


          def draw_tesseract(
              draw: ImageDraw.ImageDraw,
              center: tuple[float, float],
              scale: float,
              angles: tuple[float, ...],
              seed: str,
              width: int,
          ) -> None:
              vertices = np.array(
                  [
                      [
                          1.0 if index & (1 << axis) else -1.0
                          for axis in range(4)
                      ]
                      for index in range(16)
                  ],
                  dtype=np.float64,
              )
              projected = project_4d(
                  vertices,
                  angles,
                  center,
                  scale,
              )

              for source in range(16):
                  for axis in range(4):
                      target = source ^ (1 << axis)
                      if target <= source:
                          continue
                      color = quantized_color(
                          f"{seed}:{axis}",
                          24,
                      )
                      draw.line(
                          (
                              projected[source],
                              projected[target],
                          ),
                          fill=color,
                          width=width,
                      )

              for index, point in enumerate(projected):
                  radius = max(
                      2,
                      width * (2 if index % 5 == 0 else 1),
                  )
                  color = quantized_color(
                      f"{seed}:vertex:{index}",
                      32,
                  )
                  draw.ellipse(
                      (
                          point[0] - radius,
                          point[1] - radius,
                          point[0] + radius,
                          point[1] + radius,
                      ),
                      fill=color,
                  )


          def draw_clifford_torus(
              draw: ImageDraw.ImageDraw,
              center: tuple[float, float],
              scale: float,
              angles: tuple[float, ...],
              seed: str,
              line_width: int,
              curve_count: int,
          ) -> None:
              samples = 720
              parameter = np.linspace(
                  0.0,
                  2.0 * math.pi,
                  samples,
                  endpoint=True,
              )
              root_two = math.sqrt(2.0)

              for curve_index in range(curve_count):
                  fixed = (
                      2.0
                      * math.pi
                      * curve_index
                      / curve_count
                  )
                  first_family = np.column_stack(
                      (
                          np.cos(parameter) / root_two,
                          np.sin(parameter) / root_two,
                          np.full(samples, math.cos(fixed) / root_two),
                          np.full(samples, math.sin(fixed) / root_two),
                      )
                  )
                  second_family = np.column_stack(
                      (
                          np.full(samples, math.cos(fixed) / root_two),
                          np.full(samples, math.sin(fixed) / root_two),
                          np.cos(parameter) / root_two,
                          np.sin(parameter) / root_two,
                      )
                  )
                  first_points = project_4d(
                      first_family,
                      angles,
                      center,
                      scale,
                  )
                  second_points = project_4d(
                      second_family,
                      angles,
                      center,
                      scale,
                  )
                  draw.line(
                      first_points,
                      fill=quantized_color(
                          f"{seed}:u:{curve_index}",
                          -8,
                      ),
                      width=line_width,
                      joint="curve",
                  )
                  draw.line(
                      second_points,
                      fill=quantized_color(
                          f"{seed}:v:{curve_index}",
                          8,
                      ),
                      width=line_width,
                      joint="curve",
                  )


          def draw_superformula_segment(
              draw: ImageDraw.ImageDraw,
              center: tuple[float, float],
              radius: float,
              start_angle: float,
              end_angle: float,
              parameters: tuple[float, float, float, float],
              color: tuple[int, int, int],
              width: int,
              phase: float,
          ) -> None:
              sample_count = max(
                  32,
                  int(
                      900
                      * abs(end_angle - start_angle)
                      / (2.0 * math.pi)
                  ),
              )
              theta = np.linspace(
                  start_angle,
                  end_angle,
                  sample_count,
              )
              m, n1, n2, n3 = parameters
              radial = superformula_radius(
                  theta + phase,
                  m,
                  n1,
                  n2,
                  n3,
              )
              radial /= max(
                  1.0e-8,
                  float(np.max(radial)),
              )
              radial = radius * (
                  0.72 + 0.28 * radial
              )
              points = [
                  (
                      int(
                          round(
                              center[0]
                              + radial_value
                              * math.cos(angle)
                          )
                      ),
                      int(
                          round(
                              center[1]
                              + radial_value
                              * math.sin(angle)
                          )
                      ),
                  )
                  for angle, radial_value in zip(
                      theta,
                      radial,
                  )
              ]
              if len(points) > 1:
                  draw.line(
                      points,
                      fill=color,
                      width=width,
                      joint="curve",
                  )


          def golden_pentagram_vertices(
              center: tuple[float, float],
              radius: float,
              rotation: float,
          ) -> tuple[list[tuple[int, int]], list[tuple[int, int]]]:
              outer = []
              inner = []
              phi = (
                  math.e ** (1j * math.pi / 5)
                  + math.e ** (-1j * math.pi / 5)
              ).real
              inner_radius = radius / (phi * phi)

              for index in range(5):
                  outer_angle = (
                      rotation
                      - math.pi / 2
                      + 2.0 * math.pi * index / 5.0
                  )
                  inner_angle = (
                      rotation
                      - math.pi / 2
                      + math.pi / 5
                      + 2.0 * math.pi * index / 5.0
                  )
                  outer.append(
                      (
                          int(
                              round(
                                  center[0]
                                  + radius * math.cos(outer_angle)
                              )
                          ),
                          int(
                              round(
                                  center[1]
                                  + radius * math.sin(outer_angle)
                              )
                          ),
                      )
                  )
                  inner.append(
                      (
                          int(
                              round(
                                  center[0]
                                  + inner_radius * math.cos(inner_angle)
                              )
                          ),
                          int(
                              round(
                                  center[1]
                                  + inner_radius * math.sin(inner_angle)
                              )
                          ),
                      )
                  )

              return outer, inner


          def draw_logarithmic_spiral(
              draw: ImageDraw.ImageDraw,
              center: tuple[float, float],
              base_radius: float,
              growth: float,
              rotation: float,
              color: tuple[int, int, int],
              width: int,
              mirrored: bool,
          ) -> None:
              samples = 1200
              theta = np.linspace(
                  -1.45 * math.pi,
                  1.45 * math.pi,
                  samples,
                  dtype=np.float64,
              )
              exponent = growth * theta
              if mirrored:
                  exponent = -exponent
              radial = base_radius * np.exp(exponent / math.pi)
              points = [
                  (
                      int(
                          round(
                              center[0]
                              + radial_value
                              * math.cos(angle + rotation)
                          )
                      ),
                      int(
                          round(
                              center[1]
                              + radial_value
                              * math.sin(angle + rotation)
                          )
                      ),
                  )
                  for angle, radial_value in zip(theta, radial)
              ]
              if len(points) > 1:
                  draw.line(
                      points,
                      fill=color,
                      width=width,
                      joint="curve",
                  )


          def render_carrier(
              side: int,
              packet_header_bytes: int,
              capsule_manifest_bytes: int,
              manifest: dict,
              packet_bytes: int,
          ) -> Image.Image:
              phi = float(
                  (
                      math.e ** (1j * math.pi / 5)
                      + math.e ** (-1j * math.pi / 5)
                  ).real
              )
              n = float(
                  (
                      phi
                      + math.sqrt(phi * phi + 4.0)
                  )
                  / 2.0
              )

              root_seed = bytes.fromhex(
                  manifest["codebaseRoot"]
              )
              art_side = min(
                  side,
                  2048,
              )
              axis = np.linspace(
                  -1.0,
                  1.0,
                  art_side,
                  dtype=np.float64,
              )
              x_coordinate = axis[None, :]
              y_coordinate = axis[:, None]
              radius = np.sqrt(
                  x_coordinate * x_coordinate
                  + y_coordinate * y_coordinate
              )
              theta = np.arctan2(
                  y_coordinate,
                  x_coordinate,
              )
              phase = (
                  root_seed[0] / 255.0
              ) * 2.0 * math.pi

              beta = math.log(n) / (math.pi / 5.0)
              normalized_spiral = np.exp(
                  beta * theta / math.pi
              )
              spiral_radius = (
                  0.14
                  + 0.18
                  * (
                      normalized_spiral
                      - np.min(normalized_spiral)
                  )
                  / max(
                      1.0e-8,
                      float(
                          np.max(normalized_spiral)
                          - np.min(normalized_spiral)
                      ),
                  )
              )

              spiral_distance = np.full_like(
                  radius,
                  1.0e9,
              )
              for branch in range(10):
                  offset = phase + branch * math.pi / 5.0
                  local = (
                      (theta - offset + math.pi)
                      % (2.0 * math.pi)
                  ) - math.pi
                  branch_wave = np.exp(
                      beta * local / math.pi
                  )
                  branch_radius = (
                      0.10
                      + 0.72
                      * (
                          branch_wave
                          - np.min(branch_wave)
                      )
                      / max(
                          1.0e-8,
                          float(
                              np.max(branch_wave)
                              - np.min(branch_wave)
                          ),
                      )
                  )
                  spiral_distance = np.minimum(
                      spiral_distance,
                      np.abs(radius - branch_radius),
                  )

              pentawave = (
                  0.5
                  + 0.5
                  * np.cos(5.0 * (theta - phase))
              )
              decawave = (
                  0.5
                  + 0.5
                  * np.cos(10.0 * (theta - phase))
              )
              star_boundary = (
                  0.22
                  + 0.42
                  * np.power(
                      pentawave,
                      1.0 / phi,
                  )
              )
              star_shell = np.exp(
                  -24.0
                  * np.abs(radius - star_boundary)
              )
              spiral_shell = np.exp(
                  -36.0 * spiral_distance
              )
              resonance = np.cos(
                  10.0 * (theta - phase)
                  + phi * radius * math.pi
              )
              inner_core = np.exp(
                  -3.8 * radius * radius
              )
              outer_ring = np.exp(
                  -42.0
                  * np.abs(radius - (0.88 / phi))
              )

              red = np.clip(
                  16.0
                  + 44.0 * inner_core
                  + 68.0 * star_shell
                  + 36.0 * spiral_shell
                  + 18.0 * decawave,
                  0.0,
                  252.0,
              )
              green = np.clip(
                  20.0
                  + 34.0 * inner_core
                  + 56.0 * spiral_shell
                  + 42.0 * star_shell
                  + 24.0 * outer_ring,
                  0.0,
                  252.0,
              )
              blue = np.clip(
                  28.0
                  + 46.0 * inner_core
                  + 72.0 * outer_ring
                  + 34.0 * star_shell
                  + 24.0 * (0.5 + 0.5 * resonance),
                  0.0,
                  252.0,
              )

              array = np.stack(
                  (red, green, blue),
                  axis=2,
              ).astype(np.uint8)
              array &= VISIBLE_CHANNEL_MASK

              image = Image.fromarray(
                  array,
                  mode="RGB",
              )
              draw = ImageDraw.Draw(image)

              center = (
                  art_side / 2.0,
                  art_side / 2.0,
              )
              line_width = max(
                  1,
                  art_side // 700,
              )

              group_totals: dict[str, int] = {}
              file_rows = []
              for entry in manifest["entries"]:
                  if entry["type"] != "file":
                      continue
                  path = PurePosixPath(entry["path"])
                  group = (
                      path.parts[0]
                      if path.parts
                      else "root"
                  )
                  size = int(entry["compressedSize"])
                  group_totals[group] = (
                      group_totals.get(group, 0)
                      + size
                  )
                  file_rows.append(
                      (
                          size,
                          group,
                          entry["sha256"],
                      )
                  )

              total_group_bytes = max(
                  1,
                  sum(group_totals.values()),
              )

              for layer in range(4):
                  radius_px = art_side * (
                      0.46 / (phi ** layer)
                  )
                  outer, inner = golden_pentagram_vertices(
                      center,
                      radius_px,
                      phase / phi + layer * math.pi / 10.0,
                  )
                  outer_color = quantized_color(
                      f"outer:{layer}:{manifest['codebaseRoot']}",
                      10 * layer,
                  )
                  inner_color = quantized_color(
                      f"inner:{layer}:{manifest['codebaseRoot']}",
                      -6 * layer,
                  )

                  draw.line(
                      outer + [outer[0]],
                      fill=outer_color,
                      width=max(1, line_width + 1),
                      joint="curve",
                  )
                  star_order = [
                      outer[index]
                      for index in (0, 2, 4, 1, 3, 0)
                  ]
                  draw.line(
                      star_order,
                      fill=inner_color,
                      width=max(1, line_width + 1),
                      joint="curve",
                  )
                  draw.line(
                      inner + [inner[0]],
                      fill=outer_color,
                      width=line_width,
                      joint="curve",
                  )

              spiral_base = art_side * 0.032 * phi
              spiral_width = max(
                  1,
                  art_side // 900,
              )
              for arm in range(5):
                  angle = phase + 2.0 * math.pi * arm / 5.0
                  color_a = quantized_color(
                      f"spiral:a:{arm}:{manifest['codebaseRoot']}",
                      12,
                  )
                  color_b = quantized_color(
                      f"spiral:b:{arm}:{manifest['codebaseRoot']}",
                      -12,
                  )
                  draw_logarithmic_spiral(
                      draw,
                      center,
                      spiral_base,
                      beta,
                      angle,
                      color_a,
                      spiral_width,
                      mirrored=False,
                  )
                  draw_logarithmic_spiral(
                      draw,
                      center,
                      spiral_base / n,
                      beta,
                      angle + math.pi / 5.0,
                      color_b,
                      spiral_width,
                      mirrored=True,
                  )

              angle_cursor = -math.pi / 2.0
              orbit_radius = art_side * 0.49
              for group, weight in sorted(
                  group_totals.items(),
                  key=lambda item: (
                      -item[1],
                      item[0],
                  ),
              ):
                  span = (
                      2.0
                      * math.pi
                      * weight
                      / total_group_bytes
                  )
                  mid_angle = angle_cursor + span / 2.0
                  node_radius = max(
                      3,
                      int(
                          art_side
                          * (
                              0.003
                              + 0.012
                              * weight
                              / total_group_bytes
                              * phi
                          )
                      ),
                  )
                  x = int(
                      round(
                          center[0]
                          + orbit_radius
                          * math.cos(mid_angle + phase / n)
                      )
                  )
                  y = int(
                      round(
                          center[1]
                          + orbit_radius
                          * math.sin(mid_angle + phase / n)
                      )
                  )
                  draw.ellipse(
                      (
                          x - node_radius,
                          y - node_radius,
                          x + node_radius,
                          y + node_radius,
                      ),
                      fill=quantized_color(
                          f"group-node:{group}",
                          18,
                      ),
                  )
                  draw.line(
                      (
                          int(center[0]),
                          int(center[1]),
                          x,
                          y,
                      ),
                      fill=quantized_color(
                          f"group-line:{group}",
                          -8,
                      ),
                      width=1,
                  )
                  angle_cursor += span

              top_files = sorted(
                  file_rows,
                  key=lambda row: (
                      -row[0],
                      row[1],
                      row[2],
                  ),
              )[:180]
              for index, (size, group, digest) in enumerate(
                  top_files
              ):
                  angle = (
                      phase
                      + (index % 10) * math.pi / 5.0
                      + (index // 10) * (math.pi / (20.0 * phi))
                  )
                  radial = art_side * (
                      0.18
                      + 0.62
                      * (
                          (index + 1)
                          / max(1, len(top_files))
                      )
                  )
                  x = int(
                      round(
                          center[0] + radial * math.cos(angle)
                      )
                  )
                  y = int(
                      round(
                          center[1] + radial * math.sin(angle)
                      )
                  )
                  radius_px = max(
                      1,
                      int(
                          1
                          + art_side
                          * 0.005
                          * math.log1p(size)
                          / max(1.0, math.log1p(packet_bytes))
                      ),
                  )
                  draw.ellipse(
                      (
                          x - radius_px,
                          y - radius_px,
                          x + radius_px,
                          y + radius_px,
                      ),
                      fill=quantized_color(
                          f"{group}:{digest}",
                          6,
                      ),
                  )

              if art_side != side:
                  image = image.resize(
                      (side, side),
                      resample=Image.Resampling.LANCZOS,
                  )

              final_array = np.asarray(
                  image,
                  dtype=np.uint8,
              ).copy()
              final_array &= VISIBLE_CHANNEL_MASK

              return Image.fromarray(
                  final_array,
                  mode="RGB",
              )


          def iter_six_bit_symbol_chunks(
              packet_path: Path,
              chunk_bytes: int = 3 * 1024 * 1024,
          ):
              remainder = b""

              with packet_path.open("rb") as handle:
                  while True:
                      incoming = handle.read(chunk_bytes)
                      if not incoming:
                          break

                      data = remainder + incoming
                      complete = (len(data) // 3) * 3
                      block = data[:complete]
                      remainder = data[complete:]

                      if block:
                          values = np.frombuffer(
                              block,
                              dtype=np.uint8,
                          ).reshape(-1, 3)

                          symbols = np.empty(
                              values.shape[0] * 4,
                              dtype=np.uint8,
                          )
                          first = values[:, 0]
                          second = values[:, 1]
                          third = values[:, 2]

                          symbols[0::4] = first >> 2
                          symbols[1::4] = (
                              ((first & 0x03) << 4)
                              | (second >> 4)
                          )
                          symbols[2::4] = (
                              ((second & 0x0F) << 2)
                              | (third >> 6)
                          )
                          symbols[3::4] = third & 0x3F

                          yield symbols

              if remainder:
                  padded = remainder + b"\x00" * (3 - len(remainder))
                  values = np.frombuffer(
                      padded,
                      dtype=np.uint8,
                  )
                  first = int(values[0])
                  second = int(values[1])
                  third = int(values[2])

                  symbols = np.array(
                      [
                          first >> 2,
                          ((first & 0x03) << 4) | (second >> 4),
                          ((second & 0x0F) << 2) | (third >> 6),
                          third & 0x3F,
                      ],
                      dtype=np.uint8,
                  )

                  required = math.ceil(
                      len(remainder) * 8 / 6
                  )
                  yield symbols[:required]


          def embed_packet(
              carrier: Image.Image,
              packet_path: Path,
              symbol_count: int,
          ) -> Image.Image:
              pixels = np.asarray(
                  carrier,
                  dtype=np.uint8,
              ).copy().reshape(-1, 3)

              if symbol_count > len(pixels):
                  raise RuntimeError(
                      "carrier capacity failure: "
                      f"need {symbol_count:,} pixels, "
                      f"have {len(pixels):,}"
                  )

              pixels &= VISIBLE_CHANNEL_MASK
              cursor = 0

              for symbols in iter_six_bit_symbol_chunks(
                  packet_path
              ):
                  end = cursor + len(symbols)
                  target = pixels[cursor:end]
                  target[:, 0] |= (symbols >> 4) & 0x03
                  target[:, 1] |= (symbols >> 2) & 0x03
                  target[:, 2] |= symbols & 0x03
                  cursor = end

              if cursor != symbol_count:
                  raise RuntimeError(
                      "encoder symbol count mismatch: "
                      f"{cursor} != {symbol_count}"
                  )

              return Image.fromarray(
                  pixels.reshape(
                      carrier.height,
                      carrier.width,
                      3,
                  ),
                  mode="RGB",
              )


          def append_private_decoder_chunk(
              image_path: Path,
              decoder_source: bytes,
          ) -> None:
              chunk_type = b"drMd"
              chunk_data = (
                  b"DRMDEC01"
                  + zlib.compress(
                      decoder_source,
                      level=9,
                  )
              )
              chunk = (
                  struct.pack(
                      ">I",
                      len(chunk_data),
                  )
                  + chunk_type
                  + chunk_data
                  + struct.pack(
                      ">I",
                      zlib.crc32(
                          chunk_type + chunk_data
                      )
                      & 0xFFFFFFFF,
                  )
              )

              iend = (
                  b"\x00\x00\x00\x00"
                  b"IEND"
                  b"\xaeB`\x82"
              )
              png = image_path.read_bytes()
              position = png.rfind(iend)

              if position < 0:
                  raise RuntimeError(
                      "generated PNG is missing IEND"
                  )

              image_path.write_bytes(
                  png[:position]
                  + chunk
                  + png[position:]
              )


          def main() -> int:
              parser = argparse.ArgumentParser()
              parser.add_argument(
                  "--root",
                  type=Path,
                  required=True,
              )
              parser.add_argument(
                  "--output",
                  type=Path,
                  required=True,
              )
              parser.add_argument(
                  "--decoder",
                  type=Path,
                  required=True,
              )
              parser.add_argument(
                  "--maximum-dimension",
                  type=int,
                  default=16000,
              )
              args = parser.parse_args()

              root = args.root.resolve()
              output = args.output.resolve()
              decoder_source = args.decoder.read_bytes()
              output.parent.mkdir(
                  parents=True,
                  exist_ok=True,
              )

              with tempfile.TemporaryDirectory(
                  prefix="dream-pixel-atlas-"
              ) as temporary_text:
                  temporary = Path(temporary_text)
                  capsule_path = temporary / "codebase.capsule"
                  packet_path = temporary / "image.packet"

                  manifest, manifest_bytes = build_capsule(
                      root,
                      capsule_path,
                      decoder_source,
                      temporary,
                  )

                  packet_header = {
                      "format": "dream-pixel-codebase-atlas",
                      "version": 1,
                      "payloadBytes": capsule_path.stat().st_size,
                      "payloadSha256": sha256_file(capsule_path),
                      "codebaseRoot": manifest["codebaseRoot"],
                      "entryCount": manifest["entryCount"],
                      "carrier": (
                          "row-major RGB; two least-significant "
                          "bits per channel; six payload bits per pixel"
                      ),
                  }
                  packet_header_bytes = canonical_json(
                      packet_header
                  )

                  with packet_path.open("wb") as packet_handle:
                      packet_handle.write(
                          PACKET_PREFIX.pack(
                              PACKET_MAGIC,
                              len(packet_header_bytes),
                          )
                      )
                      packet_handle.write(packet_header_bytes)

                      with capsule_path.open("rb") as capsule_handle:
                          shutil.copyfileobj(
                              capsule_handle,
                              packet_handle,
                              length=1024 * 1024,
                          )

                  packet_bytes = packet_path.stat().st_size
                  required_pixels = math.ceil(
                      packet_bytes * 8
                      / CARRIER_BITS_PER_PIXEL
                  )
                  required_side = math.ceil(
                      math.sqrt(required_pixels)
                  )
                  side = max(
                      1536,
                      ((required_side + 63) // 64) * 64,
                  )

                  if side > args.maximum_dimension:
                      raise RuntimeError(
                          "the repository does not fit in one PNG at "
                          f"{args.maximum_dimension}x"
                          f"{args.maximum_dimension}. "
                          f"Required minimum is approximately "
                          f"{required_side}x{required_side}."
                      )

                  carrier = render_carrier(
                      side,
                      PACKET_PREFIX.size
                      + len(packet_header_bytes),
                      manifest_bytes,
                      manifest,
                      packet_bytes,
                  )
                  encoded = embed_packet(
                      carrier,
                      packet_path,
                      math.ceil(
                          packet_bytes * 8
                          / CARRIER_BITS_PER_PIXEL
                      ),
                  )

                  encoded.save(
                      output,
                      "PNG",
                      compress_level=6,
                      optimize=False,
                  )
                  append_private_decoder_chunk(
                      output,
                      decoder_source,
                  )

              print(
                  json.dumps(
                      {
                          "artifact": str(output),
                          "artifactBytes": output.stat().st_size,
                          "artifactSha256": sha256_file(output),
                          "width": side,
                          "height": side,
                          "pixelPacketBytes": packet_bytes,
                          "entries": manifest["entryCount"],
                          "codebaseRoot": manifest["codebaseRoot"],
                      },
                      indent=2,
                      sort_keys=True,
                  )
              )
              return 0


          if __name__ == "__main__":
              raise SystemExit(main())
          PY

          python -m py_compile \
            "$RUNNER_TEMP/build_dream_pixel_atlas.py"

          rm -rf "$GITHUB_WORKSPACE/dream-pixel-output"
          mkdir -p "$GITHUB_WORKSPACE/dream-pixel-output"

          python "$RUNNER_TEMP/build_dream_pixel_atlas.py" \
            --root "$GITHUB_WORKSPACE" \
            --output "$GITHUB_WORKSPACE/dream-pixel-output/DREAM_CODEBASE_SPATIAL_ATLAS.png" \
            --decoder "$RUNNER_TEMP/dream_pixel_restore.py" \
            --maximum-dimension "$MAXIMUM_DIMENSION" \
            | tee "$RUNNER_TEMP/dream-pixel-build.json"

      - name: Prove exact recovery starting with only the PNG
        shell: bash
        run: |
          set -euo pipefail

          IMAGE="$GITHUB_WORKSPACE/dream-pixel-output/DREAM_CODEBASE_SPATIAL_ATLAS.png"
          PROOF_ROOT="$(mktemp -d)"
          trap 'rm -rf "$PROOF_ROOT"' EXIT

          cp "$IMAGE" "$PROOF_ROOT/DREAM_CODEBASE_SPATIAL_ATLAS.png"
          cd "$PROOF_ROOT"

          test "$(find . -mindepth 1 -maxdepth 1 | wc -l)" -eq 1
          test -f DREAM_CODEBASE_SPATIAL_ATLAS.png

          python - \
            DREAM_CODEBASE_SPATIAL_ATLAS.png \
            rebuilt-codebase \
            restore <<'PY'
          from __future__ import annotations

          import struct
          import sys
          import zlib
          from pathlib import Path

          PNG_SIGNATURE = b"\x89PNG\r\n\x1a\n"

          image = Path(sys.argv[1])
          output = sys.argv[2]
          command = sys.argv[3]

          data = image.read_bytes()
          if data[:8] != PNG_SIGNATURE:
              raise SystemExit("not a PNG")

          cursor = 8
          decoder = None

          while cursor < len(data):
              if cursor + 12 > len(data):
                  raise SystemExit("truncated PNG")

              length = struct.unpack(
                  ">I",
                  data[cursor : cursor + 4],
              )[0]
              chunk_type = data[
                  cursor + 4 : cursor + 8
              ]
              chunk_data = data[
                  cursor + 8 : cursor + 8 + length
              ]
              cursor += 12 + length

              if chunk_type == b"drMd":
                  if not chunk_data.startswith(
                      b"DRMDEC01"
                  ):
                      raise SystemExit(
                          "invalid decoder chunk"
                      )
                  decoder = zlib.decompress(
                      chunk_data[8:]
                  )
                  break

              if chunk_type == b"IEND":
                  break

          if decoder is None:
              raise SystemExit(
                  "embedded decoder chunk is missing"
              )

          sys.argv = [
              "dream_pixel_restore.py",
              command,
              str(image),
              output,
          ]
          exec(
              compile(
                  decoder,
                  "dream_pixel_restore.py",
                  "exec",
              ),
              {
                  "__name__": "__main__",
                  "__file__": "dream_pixel_restore.py",
              },
          )
          PY

          cat > "$RUNNER_TEMP/verify_pixel_roundtrip.py" <<'PY'
          from __future__ import annotations

          import hashlib
          import os
          import stat
          import subprocess
          import sys
          from pathlib import Path, PurePosixPath

          source = Path(sys.argv[1]).resolve()
          rebuilt = Path(sys.argv[2]).resolve()

          result = subprocess.run(
              [
                  "git",
                  "-C",
                  str(source),
                  "ls-files",
                  "-z",
                  "--recurse-submodules",
              ],
              check=True,
              stdout=subprocess.PIPE,
          )

          expected: set[str] = set()

          for raw in result.stdout.split(b"\0"):
              if not raw:
                  continue

              relative_text = raw.decode(
                  "utf-8",
                  "surrogateescape",
              )
              relative = PurePosixPath(relative_text)
              original = source.joinpath(*relative.parts)

              if original.is_dir() and not original.is_symlink():
                  continue

              expected.add(relative.as_posix())
              restored = rebuilt.joinpath(*relative.parts)

              if original.is_symlink():
                  if not restored.is_symlink():
                      raise SystemExit(
                          f"missing symlink: {relative}"
                      )
                  if os.readlink(original) != os.readlink(restored):
                      raise SystemExit(
                          f"symlink target mismatch: {relative}"
                      )
                  continue

              if not restored.is_file() or restored.is_symlink():
                  raise SystemExit(
                      f"missing regular file: {relative}"
                  )

              if (
                  hashlib.sha256(
                      original.read_bytes()
                  ).digest()
                  != hashlib.sha256(
                      restored.read_bytes()
                  ).digest()
              ):
                  raise SystemExit(
                      f"byte mismatch: {relative}"
                  )

              original_mode = stat.S_IMODE(
                  os.lstat(original).st_mode
              )
              restored_mode = stat.S_IMODE(
                  os.lstat(restored).st_mode
              )
              if original_mode != restored_mode:
                  raise SystemExit(
                      f"mode mismatch: {relative}: "
                      f"{oct(original_mode)} != "
                      f"{oct(restored_mode)}"
                  )

          actual: set[str] = set()

          for directory, dirnames, filenames in os.walk(
              rebuilt,
              followlinks=False,
          ):
              base = Path(directory)

              for dirname in list(dirnames):
                  item = base / dirname
                  if item.is_symlink():
                      actual.add(
                          item.relative_to(
                              rebuilt
                          ).as_posix()
                      )
                      dirnames.remove(dirname)

              for filename in filenames:
                  actual.add(
                      (base / filename)
                      .relative_to(rebuilt)
                      .as_posix()
                  )

          if expected != actual:
              missing = sorted(expected - actual)
              extra = sorted(actual - expected)
              raise SystemExit(
                  "tree mismatch; "
                  f"missing={missing[:20]} "
                  f"extra={extra[:20]}"
              )

          print(
              "EXACT_PIXEL_ROUNDTRIP_OK "
              f"entries={len(expected)}"
          )
          PY

          python "$RUNNER_TEMP/verify_pixel_roundtrip.py" \
            "$GITHUB_WORKSPACE" \
            "$PROOF_ROOT/rebuilt-codebase"

          {
            echo "## DREAM pixel recovery proof"
            echo
            echo "The proof directory began with exactly one file:"
            echo
            echo "\`DREAM_CODEBASE_SPATIAL_ATLAS.png\`"
            echo
            echo "The repository capsule was decoded from the RGB low-bit pixel plane."
            echo "The PNG metadata supplied only the bootstrap decoder, and that decoder was also verified against the copy stored inside the pixel payload."
            echo "Every tracked file byte, executable mode, and symlink target matched the checkout."
            echo
            echo "The original PNG is mandatory. JPEG conversion, resizing, screenshots, cropping, or image optimization destroys the encoded pixel payload."
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload the one-image recovery artifact
        uses: actions/upload-artifact@v4
        with:
          name: DREAM_CODEBASE_SPATIAL_ATLAS
          path: dream-pixel-output/DREAM_CODEBASE_SPATIAL_ATLAS.png
          if-no-files-found: error
          compression-level: 0
          retention-days: 30
