'use client'; import type { ContentAsset } from '@/lib/contentengin/assetTypes';
export default function ExportPanel({asset,downloads}:{asset:ContentAsset|null;downloads:{model?:string;zip?:string;manifest?:string}|null}){return <section className="ce-card"><h2>Validation & Export</h2>{asset?<><p>{asset.validation.gameReady?'Game-ready':'Needs fixes'} · {asset.validation.errors.length} errors · {asset.validation.warnings.length} warnings</p><div className="ce-actions">{downloads?.model&&<a href={downloads.model} download>Download GLB</a>}{downloads?.manifest&&<a href={downloads.manifest} download>Download Manifest</a>}{downloads?.zip&&<a href={downloads.zip} download>Download ZIP Bundle</a>}</div><p>Exports are file downloads; assets and source images are not saved to the database after download.</p></>:<p>Build an asset to validate and download one file or a ZIP bundle.</p>}</section>}
