'use client'; import type { ContentRecipe, ExportProfile } from '@/engins/contentengin/assetTypes';
const types=['humanoid','quadruped','bird','fish','creature','car','truck','bicycle','motorcycle','building','road','bridge','terrain','tree','water','weapon','furniture','container','tool'];
export default function RecipeEditor({recipe,onChange}:{recipe:ContentRecipe;onChange:(r:ContentRecipe)=>void}){ return <section className="ce-card"><h2>Recipe</h2><label>Asset type<select value={recipe.assetType} onChange={e=>onChange({...recipe,assetType:e.target.value})}>{types.map(t=><option key={t}>{t}</option>)}</select></label><label>Seed<input type="number" value={recipe.seed} onChange={e=>onChange({...recipe,seed:Number(e.target.value)})}/></label><label>Profile<select value={recipe.profile} onChange={e=>onChange({...recipe,profile:e.target.value as ExportProfile})}><option>ps3</option><option>ps4</option><option>ps5</option></select></label><p>Same recipe, seed, profile, and engine version generates the same editable asset.</p></section> }
