'use client'; import { useRef } from 'react';
export default function PhotoReferencePanel({onAnalysis}:{onAnalysis:(a:unknown)=>void}){ const ref=useRef<HTMLInputElement>(null); async function upload(file:File){ const fd=new FormData(); fd.append('file',file); const res=await fetch('/api/contentengin/upload',{method:'POST',body:fd}); const data=await res.json(); onAnalysis(data.sourceAnalysis); } return <section className="ce-card"><h2>Photo Reference</h2><input ref={ref} type="file" accept="image/*" onChange={e=>{const f=e.target.files?.[0]; if(f) void upload(f)}}/><p>Photos are analyzed as temporary reference data only. Source images are not stored in the database and are not retained after export.</p></section>}
