# Concept Patterns

Reusable building blocks the **Game Architect** mixes when drafting a
Vision Statement. Three flavours: `settings/`, `protagonists/`,
`scope-formulas/`. Each file is flat JSON with `pattern_id` + `category`.
