import { mkdir, writeFile, readFile, readdir, stat } from 'fs/promises'; import path from 'path'; import { ContentAsset } from '../assetTypes'; import { createGlbBuffer } from './exportGlb'; import { validateAsset } from './validate'; import { makeManifest } from './writeManifest';
const crcTable=Array.from({length:256},(_,n)=>{let c=n; for(let k=0;k<8;k++) c=((c&1)?(0xedb88320^(c>>>1)):(c>>>1)); return c>>>0;});
function crc32(buf:Buffer){let c=0xffffffff; for(const b of buf)c=crcTable[(c^b)&255]!^(c>>>8); return (c^0xffffffff)>>>0;}
export async function writeAssetBundle(asset:ContentAsset,outDir:string){ await mkdir(outDir,{recursive:true}); const glb=createGlbBuffer(asset); asset.validation=validateAsset(asset, glb); await writeFile(path.join(outDir,'model.glb'),glb); await writeFile(path.join(outDir,'manifest.json'),JSON.stringify(makeManifest(asset),null,2)); await writeFile(path.join(outDir,'recipe.json'),JSON.stringify(asset.recipe,null,2)); await writeFile(path.join(outDir,'validation.json'),JSON.stringify(asset.validation,null,2)); await writeFile(path.join(outDir,'thumbnail.webp'),Buffer.from('RIFF\x1a\x00\x00\x00WEBPVP8 \x0e\x00\x00\x00\x2f\x00\x00\x00\x10\x07\x10\x11\x11\x88\x88\xfe\x07\x00','binary')); if(asset.recipe.sourceImage) await writeFile(path.join(outDir,'source_analysis.json'),JSON.stringify(asset.recipe.sourceImage,null,2)); return outDir; }
export async function zipDirectory(dir:string,zipPath:string){ const files:string[]=[]; async function walk(d:string){ for(const e of await readdir(d)){ const f=path.join(d,e); if((await stat(f)).isDirectory()) await walk(f); else files.push(f); } } await walk(dir); const chunks:Buffer[]=[]; const central:Buffer[]=[]; let offset=0; for(const file of files){ const data=await readFile(file); const name=Buffer.from(path.relative(dir,file).replace(/\\/g,'/')); const crc=crc32(data); const local=Buffer.alloc(30+name.length); local.writeUInt32LE(0x04034b50,0); local.writeUInt16LE(20,4); local.writeUInt16LE(0,6); local.writeUInt16LE(0,8); local.writeUInt32LE(0,10); local.writeUInt32LE(crc,14); local.writeUInt32LE(data.length,18); local.writeUInt32LE(data.length,22); local.writeUInt16LE(name.length,26); name.copy(local,30); chunks.push(local,data); const cen=Buffer.alloc(46+name.length); cen.writeUInt32LE(0x02014b50,0); cen.writeUInt16LE(20,4); cen.writeUInt16LE(20,6); cen.writeUInt16LE(0,8); cen.writeUInt16LE(0,10); cen.writeUInt32LE(0,12); cen.writeUInt32LE(crc,16); cen.writeUInt32LE(data.length,20); cen.writeUInt32LE(data.length,24); cen.writeUInt16LE(name.length,28); cen.writeUInt32LE(offset,42); name.copy(cen,46); central.push(cen); offset+=local.length+data.length; } const centralSize=central.reduce((n,b)=>n+b.length,0); const end=Buffer.alloc(22); end.writeUInt32LE(0x06054b50,0); end.writeUInt16LE(files.length,8); end.writeUInt16LE(files.length,10); end.writeUInt32LE(centralSize,12); end.writeUInt32LE(offset,16); await writeFile(zipPath,Buffer.concat([...chunks,...central,end])); return zipPath; }
