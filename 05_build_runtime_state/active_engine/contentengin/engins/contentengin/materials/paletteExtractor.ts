export function rgbaToHex(r:number,g:number,b:number):string { return `#${[r,g,b].map(v=>Math.max(0,Math.min(255,Math.round(v))).toString(16).padStart(2,'0')).join('')}`; }
export function extractPalette(bytes:Uint8Array, max=6):string[]{ const step=Math.max(1,Math.floor(bytes.length/(max*512))); const bins=new Map<string,number>(); for(let i=0;i<bytes.length;i+=step){ const v=bytes[i]!; const key=rgbaToHex(v, bytes[(i+17)%bytes.length]!, bytes[(i+31)%bytes.length]!); bins.set(key,(bins.get(key)??0)+1); } return [...bins.entries()].sort((a,b)=>b[1]-a[1]).slice(0,max).map(([k])=>k); }
