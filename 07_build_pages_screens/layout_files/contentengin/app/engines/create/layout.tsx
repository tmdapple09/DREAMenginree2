import type { ReactNode } from 'react';

// SURFACE: dream.shell.EnginesCreateLayout  (framework-mandated basename: layout.tsx)
export const metadata = { title: 'ContentEngin – DREAMengin', description: 'Full-feature content creation engine app.' };
export default function CreateEnginLayout({ children }: {children: ReactNode}) { return <>{children}</>; }
