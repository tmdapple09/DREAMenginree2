# R&D Sessions

Append-only log of every research session run by the autonomous studio team.
Filename pattern: `YYYY-MM-DD-{agent}-{topic}-{ISO-timestamp}.json`.

Sessions older than 90 days may be compacted into an annual digest.
