// AUTO-GENERATED by scripts/wire-orphans.mjs — do not edit by hand.

// Minimal structural type to avoid a circular dep with @/src/engin/core.
// Only the registry.hydrate surface is needed here.
interface EngineWithRegistry {
  registry: { hydrate: (config: Record<string, unknown>) => void };
}
import { brain } from './brain';
import { cartridges } from './cartridges';
import { connectors } from './connectors';
import { hooks } from './hooks';
import { personas } from './personas';
import { rulesets } from './rulesets';
import { surfaces } from './surfaces';
import { systems } from './systems';

export function hydrateEngineRegistry(engine: EngineWithRegistry): void {
  engine.registry.hydrate({
    rulesets,
    surfaces,
    connectors,
    cartridges,
    brain,
    personas,
    systems,
    hooks,
  });
}